{
  "name": "rsvp-reader",
  "version": "1.0.0",
  "description": "RSVP (Rapid Serial Visual Presentation) reader application",
  "scripts": {
    "dev": "flutter run -d chrome --web-port=3001",
    "build": "flutter build web"
  }
}